spring:
  main:
    banner-mode: "off"

logging:
  level:
    com.milkrun: INFO

milkrun:
  analytics:
    # The demo VM is small and shared; give queries time and serve cached results longer
    timeout: 30s
    cache-ttl: 60s
